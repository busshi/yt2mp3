version: '3'

services:
  nextjs:
    image: yt2mp3_dev
    container_name: yt2mp3_dev
    build:
      context: ./
      dockerfile: Dockerfile.dev
    volumes:
      - ./app:/usr/app
    ports:
      - 3000:3000
    restart: always
